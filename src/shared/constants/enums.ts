//USER ENUMS

export enum STATUS {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  BANNED = 'banned',
}

export enum ROLE {
  ADMIN = 'admin',
  USER = 'user',
}

//POST ENUMS

export enum POST_STATUS {
  BANNED = 'Banned',
  PUBLISHED = 'Published',
}

export enum POST_MEDIA_TYPE {
  IMAGE = 'Image',
  VIDEO = 'Video',
  AUDIO = 'Audio',
}
